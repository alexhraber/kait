name: Validate Kaite images

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      enable_accelerators:
        description: Enable inactive NVIDIA, AMD, and Intel host jobs
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.hardware }}-${{ matrix.variant }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - hardware: cpu
            variant: slim
            runner: ubuntu-24.04
            platform: linux/amd64
            base_image: ubuntu:24.04
            python_bin: python3
            smoke: true
          - hardware: cpu
            variant: full
            runner: ubuntu-24.04
            platform: linux/amd64
            base_image: ubuntu:24.04
            python_bin: python3
            smoke: true
          - hardware: apple
            variant: slim
            runner: ubuntu-24.04-arm
            platform: linux/arm64
            base_image: ubuntu:24.04
            python_bin: python3
            smoke: true
          - hardware: apple
            variant: full
            runner: ubuntu-24.04-arm
            platform: linux/arm64
            base_image: ubuntu:24.04
            python_bin: python3
            smoke: true
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Validate bake graph
        if: matrix.hardware == 'cpu' && matrix.variant == 'slim'
        run: docker buildx bake --print
      - name: Build and boot image contract
        env:
          HARDWARE: ${{ matrix.hardware }}
          VARIANT: ${{ matrix.variant }}
          PLATFORM: ${{ matrix.platform }}
          BASE_IMAGE: ${{ matrix.base_image }}
          PYTHON_BIN: ${{ matrix.python_bin }}
        run: |
          set -euo pipefail
          docker buildx build \
            --load \
            --platform "${PLATFORM}" \
            --tag "kaite:ci-${HARDWARE}-${VARIANT}" \
            --build-arg BASE_IMAGE="${BASE_IMAGE}" \
            --build-arg BUILDKITE_AGENT_VERSION=3.123.1 \
            --build-arg KAITE_HARDWARE="${HARDWARE}" \
            --build-arg KAITE_VARIANT="${VARIANT}" \
            --build-arg KAITE_PYTHON="${PYTHON_BIN}" \
            --provenance=false \
            --sbom=false \
            .
          docker run --rm --platform "${PLATFORM}" "kaite:ci-${HARDWARE}-${VARIANT}" doctor
      - name: Run native framework smoke test
        if: matrix.smoke
        env:
          HARDWARE: ${{ matrix.hardware }}
          VARIANT: ${{ matrix.variant }}
          PLATFORM: ${{ matrix.platform }}
        run: docker run --rm --platform "${PLATFORM}" -e KAITE_HARDWARE="${HARDWARE}" -e KAITE_VARIANT="${VARIANT}" "kaite:ci-${HARDWARE}-${VARIANT}" smoke

  build-accelerators:
    name: Build inactive accelerator ${{ matrix.hardware }}-${{ matrix.variant }}
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.enable_accelerators == true }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # These labels are deliberately inactive until matching hosts are registered.
          - hardware: nvidia
            variant: slim
            runner: kaite-nvidia
            platform: linux/amd64
            base_image: nvidia/cuda:12.6.3-cudnn-devel-ubuntu22.04
            python_bin: python3
          - hardware: nvidia
            variant: full
            runner: kaite-nvidia
            platform: linux/amd64
            base_image: nvidia/cuda:12.6.3-cudnn-devel-ubuntu22.04
            python_bin: python3
          - hardware: amd
            variant: slim
            runner: kaite-amd
            platform: linux/amd64
            base_image: rocm/dev-ubuntu-24.04:6.2.4-complete
            python_bin: python3
          - hardware: amd
            variant: full
            runner: kaite-amd
            platform: linux/amd64
            base_image: rocm/dev-ubuntu-24.04:6.2.4-complete
            python_bin: python3
          - hardware: intel
            variant: slim
            runner: kaite-intel
            platform: linux/amd64
            base_image: intel/oneapi-basekit:2025.0.1-0-devel-ubuntu22.04
            python_bin: python3.11
          - hardware: intel
            variant: full
            runner: kaite-intel
            platform: linux/amd64
            base_image: intel/oneapi-basekit:2025.0.1-0-devel-ubuntu22.04
            python_bin: python3.11
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build and smoke inactive accelerator image
        env:
          HARDWARE: ${{ matrix.hardware }}
          VARIANT: ${{ matrix.variant }}
          PLATFORM: ${{ matrix.platform }}
          BASE_IMAGE: ${{ matrix.base_image }}
          PYTHON_BIN: ${{ matrix.python_bin }}
        run: |
          set -euo pipefail
          docker buildx build \
            --load \
            --platform "${PLATFORM}" \
            --tag "kaite:ci-${HARDWARE}-${VARIANT}" \
            --build-arg BASE_IMAGE="${BASE_IMAGE}" \
            --build-arg BUILDKITE_AGENT_VERSION=3.123.1 \
            --build-arg KAITE_HARDWARE="${HARDWARE}" \
            --build-arg KAITE_VARIANT="${VARIANT}" \
            --build-arg KAITE_PYTHON="${PYTHON_BIN}" \
            --provenance=false \
            --sbom=false \
            .
          docker run --rm --platform "${PLATFORM}" "kaite:ci-${HARDWARE}-${VARIANT}" doctor
          docker run --rm --platform "${PLATFORM}" -e KAITE_HARDWARE="${HARDWARE}" -e KAITE_VARIANT="${VARIANT}" "kaite:ci-${HARDWARE}-${VARIANT}" smoke
