apiVersion: batch/v1
kind: Job
metadata:
  name: kait-agent
  labels:
    app.kubernetes.io/name: kait
    app.kubernetes.io/component: buildkite-agent
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kait
        app.kubernetes.io/component: buildkite-agent
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /metrics
        prometheus.io/port: "9090"
    spec:
      restartPolicy: Never
      containers:
        - name: kait
          image: ghcr.io/alexhraber/kait:cpu-slim
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 9090
          env:
            - name: BUILDKITE_AGENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: buildkite-agent
                  key: token
            - name: BUILDKITE_AGENT_TAGS
              value: kait=true,kait.hardware=cpu,kait.variant=slim,kait.o11y=prometheus,kait.capability.data-science=true
            - name: BUILDKITE_AGENT_QUEUE
              value: ai
            - name: BUILDKITE_AGENT_DISCONNECT_AFTER_JOB
              value: "true"
            - name: KAIT_HARDWARE
              value: cpu
            - name: KAIT_VARIANT
              value: slim
            - name: KAIT_O11Y
              value: prometheus
            - name: KAIT_METRICS_ADDR
              value: 0.0.0.0:9090
            - name: OTEL_SERVICE_NAME
              value: kait
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: kait-observability
                  key: otlp-endpoint
                  optional: true
          resources:
            requests:
              cpu: "1"
              memory: 4Gi
            limits:
              cpu: "4"
              memory: 16Gi
          volumeMounts:
            - name: builds
              mountPath: /buildkite/builds
      volumes:
        - name: builds
          emptyDir: {}
