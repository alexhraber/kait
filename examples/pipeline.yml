env:
  KAITE_O11Y: prometheus

steps:
  - label: ":mag: CPU unit tests"
    command: "kaite smoke"
    agents:
      queue: ai
      kaite: "true"
      kaite.hardware: cpu
      kaite.variant: slim

  - label: ":apple: Apple Silicon CPU evaluation"
    command: "kaite smoke"
    agents:
      queue: ai
      kaite: "true"
      kaite.hardware: apple
      kaite.variant: full

# Inactive until a matching accelerator host is deliberately enabled:
# - label: ":cuda: NVIDIA inference"
#   command: "kaite smoke"
#   agents:
#     queue: ai
#     kaite: "true"
#     kaite.hardware: nvidia
#     kaite.variant: slim
