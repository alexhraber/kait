# Linux/arm64 CPU image used on Apple Silicon hosts.
# This is Linux CPU execution; it does not claim Metal passthrough in Ubuntu.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0
